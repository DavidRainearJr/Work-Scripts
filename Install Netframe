::This looks this way because we can't right click and run .ps1 files as admin. Starting a batch file as admin them running powershell in it works fine. 

powershell -command "$mountResult = Mount-DiskImage [REDACTED].iso -PassThru ; $mountResult | Get-Volume ; Write-Host "$mountResult" ; $driveLetter = ($mountResult | Get-Volume).DriveLetter ; cls ; Write-Host Drive Mounted And NET FRAME will now be installed; pause; DISM /Online /Enable-Feature /FeatureName:NetFx3 /All /LimitAccess /Source:${driveLetter}:\sources\sxs ; Write-Host NET FRAME is now installed ; pause ; Dismount-DiskImage -ImagePath [REDACTED].iso"
